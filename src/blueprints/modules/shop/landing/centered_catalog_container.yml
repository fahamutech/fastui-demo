condition:
  modifier:
    props:
      id: centered_catalog_container
    left: ./onPins_button.yml
    frame:
      base: row.start
      styles:
        spaceValue: 0
        flexWrap: nowrap
        justifyContent: center
        alignItems: center
