condition:
  modifier:
    extend: ./catalog_container.yml
    props:
      id: divider_container
    left: ./divider_line_container.yml
    frame:
      base: column.start
      styles:
        spaceValue: 0
        flexWrap: nowrap
        justifyContent: center
        alignItems: center
        backgroundColor: rgba(0,0,0,255)
        height: 1
