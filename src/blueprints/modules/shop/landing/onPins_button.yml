condition:
  modifier:
    extend: ./onWaistChain_button.yml
    props:
      id: onPins_button
      onClick: logics.onClick
    left: ./pins_text.yml
    frame:
      base: row.start
      styles:
        spaceValue: 16
        paddingLeft: 10
        paddingRight: 10
        paddingTop: 10
        paddingBottom: 10
        flexWrap: nowrap
        justifyContent: center
        alignItems: center
        cursor: pointer
