condition:
  modifier:
    extend: ./onLogin_button.yml
    props:
      id: terms_and_condition
    left: ./onCondition_button.yml
    frame:
      base: column.start
      styles:
        spaceValue: 16
        flexWrap: nowrap
        justifyContent: center
        alignItems: normal
